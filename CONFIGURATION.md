# Example configuration

Mapbox tokens and service URLs are non-working examples. Supply your own restricted tokens and endpoints locally. Android signing keystores and Google/Firebase configuration files are intentionally excluded from Git; configure these locally before building.

Values containing `EXAMPLE`, zero-only identifiers and `example.com` domains are placeholders, not real credentials. Never use these example credentials in production.
